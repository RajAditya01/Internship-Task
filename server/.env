# MongoDB connection URI
MONGO_URI=mongodb://localhost:27017/contact

# JWT secret key for authentication
JWT_SECRET=ADITYARAJTIWARY

# Server port (optional, defaults to 5000 if not specified)
PORT=5000
